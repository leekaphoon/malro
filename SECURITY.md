# 보안

## 이 앱이 키를 다루는 방식

**브라우저 코드에는 비밀이 없습니다.** 저장소 어디에도 API 키·시크릿이 들어 있지 않고,
들어갈 수도 없게 배포 스크립트가 막습니다.

| 값 | 어디에 있나 | 브라우저가 보나 |
|---|---|---|
| Google OAuth 클라이언트 ID | 코드 (공개 값) | 예 — 설계상 공개입니다 |
| Google 액세스 토큰 | 브라우저 메모리 + `localStorage` | 예 — 그 사용자 것입니다 |
| **Gemini API 키** | **프록시 서버에만** (Apps Script Script Properties) | **아니오** |
| **공유 시크릿** | **프록시 서버 + 시리 단축어** | 설정에 입력한 기기에만 |

OAuth 클라이언트 ID 는 비밀이 아닙니다. 브라우저 OAuth(Google Identity Services)는
클라이언트 시크릿을 쓰지 않으며, 허용된 JavaScript 원본으로 오용을 막습니다.

## 배포 시 키 유출 차단

`deploy-pages.sh` 는 업로드 전에 산출물을 스캔해서 API 키 형태의 문자열이 하나라도
있으면 **배포를 중단**합니다. 손으로 확인하는 절차에 기대지 않습니다.

## 데모 모드

`?demo=1` 은 **외부 요청을 하나도 만들지 않습니다.** Google 로그인도, Gemini 호출도
없고 아무것도 저장하지 않습니다. `demotest.js` 가 요청 건수 0을 단언합니다.
공개용 스크린샷·데모 링크는 반드시 이 모드로 만드십시오.

## 알려진 한계 — 반드시 읽으십시오

### AI 백엔드는 1인용입니다

`voice/` 의 Apps Script 백엔드는 `executeAs: USER_DEPLOYING` +
`ScriptApp.getOAuthToken()` 으로 돕니다. **누가 호출하든 Tasks 접근은 스크립트 소유자
권한으로 일어납니다.** 공유 시크릿은 사용자를 구분하지 않습니다 — 문 하나를 여는
열쇠입니다.

시크릿을 남에게 주면 그 사람이 만든 태스크가 **당신의 목록에 생기고**, 그 사람이
조회하면 **당신의 할 일이 읽힙니다.** 앱 본체(PWA)는 각자 OAuth 라 멀쩡하고 AI 경로만
새기 때문에 눈치채기 어렵습니다.

**시크릿을 공유하지 마십시오.** 여러 명이 쓰려면 요청 본문에 각자의 액세스 토큰을 싣고
서버가 `getOAuthToken()` 대신 그 토큰을 쓰도록 고쳐야 합니다.

### 메타데이터가 다른 앱에 노출됩니다

우선순위·라벨·시각·반복을 담을 필드가 Google Tasks 에 없어, 메모 마지막 줄에
`⟦p1 @현장 ⏰14:00 ↻w1:1⟧` 로 인코딩합니다. 이 앱은 이 줄을 감추지만 **Google Tasks
앱·Calendar·Gmail 의 작업 패널에서는 그대로 보입니다.** 거기서 그 줄을 지우면 이 앱
입장에서는 처음부터 없었던 것이 됩니다.

### 브라우저에 남는 것

할 일 캐시와 액세스 토큰이 `localStorage` 에 남습니다. 공용 PC 에서 쓰셨다면
사이드바의 계정 전환(또는 브라우저 사이트 데이터 삭제)으로 지우십시오.

## 취약점 신고

보안 문제를 발견하시면 공개 이슈 대신 저장소 소유자에게 비공개로 알려 주십시오.
